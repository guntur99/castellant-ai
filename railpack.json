{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "touch src/main.rs",
        "SQLX_OFFLINE=true cargo build --release && mkdir -p bin && cp target/release/castellant bin/"
      ]
    }
  },
  "start": {
    "command": "./bin/castellant"
  }
}
